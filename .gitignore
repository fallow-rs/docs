launch-content/
.DS_Store
dist/
node_modules/
.fallow/

# A global .claude/ ignore hides this directory from git add. Re-include it,
# then track only the shared agent files below; local settings stay ignored.
!.claude/
.claude/*
!.claude/skills/
