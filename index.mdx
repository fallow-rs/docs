---
title: "Fallow documentation"
sidebarTitle: "Introduction"
description: "Find the code you can delete, merge, or refactor in a TypeScript or JavaScript project. Fallow analyzes code and styles statically for free, and optional runtime intelligence shows what ran in production."
keywords: ["fallow", "typescript", "javascript", "dead code", "duplication", "complexity", "architecture", "runtime intelligence", "runtime coverage", "MCP", "VS Code", "CI"]
icon: "house"
mode: "wide"
---

Fallow shows you which code in a TypeScript or JavaScript project you can delete, merge, or refactor. It is **codebase intelligence**: one command reads your whole module graph and reports what it finds.

The static layer finds:

- unused code
- duplication
- complexity hotspots
- architecture drift
- styling drift from your design system
- feature-flag branches

To learn which code executed in production, add the optional runtime layer.

## Start here

<Steps>
  <Step title="Run fallow without installing">

  ```bash
  npx fallow
  ```

  The first run needs no config.
  </Step>

  <Step title="Read the results">

  One pass reports dead code, duplication, and complexity. For styling drift, run `fallow audit` or `fallow health --css`.
  </Step>

  <Step title="Go deeper">

  ```bash
  npx fallow dead-code
  npx fallow dupes
  npx fallow health
  npx fallow fix --dry-run
  ```
  </Step>
</Steps>

## What fallow helps you do

<CardGroup cols={2}>
  <Card title="Delete cold code" icon="trash" href="/analysis/dead-code">
    Find unused files, exports, and dependencies, and remove them.
  </Card>
  <Card title="Reduce duplication" icon="clone" href="/analysis/duplication">
    Find repeated logic before it spreads.
  </Card>
  <Card title="Prioritize refactors" icon="fire" href="/explanations/health">
    Rank code by complexity and health, and fix the worst first.
  </Card>
  <Card title="Add TypeScript evidence" icon="code" href="/analysis/type-aware">
    Confirm exact symbol use, class contracts, affected files, and the tests to run.
  </Card>
  <Card title="Add runtime evidence" icon="chart-network" href="/analysis/runtime-coverage">
    See hot paths and cold paths, and back a delete with production data.
  </Card>
</CardGroup>

## Pick your workflow

<CardGroup cols={3}>
  <Card title="CLI" icon="terminal" href="/quickstart">
    Start here. The CLI runs locally, in CI, and in agent loops.
  </Card>
  <Card title="VS Code" icon="window" href="/integrations/vscode">
    Get real-time diagnostics, Code Lens, and one-click fixes in the editor.
  </Card>
  <Card title="MCP and agents" icon="robot" href="/integrations/mcp">
    Give Claude Code, Cursor, and other agents structured tool calls.
  </Card>
</CardGroup>

## Understand the two layers

Static analysis tells you which code is connected to which. Runtime intelligence tells you which code actually ran. `fallow health` shows both.

<Card title="Static vs runtime intelligence" icon="book-open" href="/explanations/static-vs-runtime">
  Learn how the two layers differ before you set up runtime collection.
</Card>

## Most-used pages

<CardGroup cols={2}>
  <Card title="Quick start" icon="rocket" href="/quickstart" />
  <Card title="Configuration" icon="gear" href="/configuration/overview" />
  <Card title="CI integration" icon="shield-check" href="/integrations/ci" />
  <Card title="CLI reference" icon="terminal" href="/cli/dead-code" />
</CardGroup>
